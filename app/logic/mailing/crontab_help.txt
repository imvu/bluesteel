
# Install your jobs with crontab:
    crontab crontab.txt


# Check the list of scheduled jobs for your user:
    crontab -l


# Reset the list of scheduled jobs for your user:
    crontab -r

# Add this at the end of the command to log cron jobs:
    >> ~/auto-update.log 2>&1